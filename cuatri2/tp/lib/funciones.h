#ifndef FUNCIONES_H
#define FUNCIONES_H

#include <stdio.h>
#include <sys/types.h>

typedef enum {
    voltaje_correcto,
    voltaje_incorrecto
} estados_t;

typedef struct{
    char volt,
    char volt_set,
    
} voltajes;

#endif
